section
  section(data-background="#282c34").dark
    h3 1. New tool, who dis?
    pre
      code.bash.
        $ git config --global user.name "norm"
        $ git config --global user.email "norm@doofenschmirtz.evil"
  section
    h3 Arbeit suchen
    p.typo_h4 http://g11013744:3210
    img.fragment(src="./img/0/screen_0.jpg")
  section
    //- p.typo_h4 Anmelden
    p mit Windows-Kennwort
    img.fragment(src="./img/0/screen_1.jpg")
  section
    //- p.typo_h4 Profil anpassen
    img.fragment(src="./img/0/screen_2.jpg")
  section
    p.typo_h4 Repo(sitory) vom Chef suchen
    img.fragment(src="./img/0/screen_3.jpg")
  section.dark(data-background="#282c34")
    h3 3. Repo lokal clonen
    pre.fragment
      code.bash.no-bg.
        $ C:\Users\doofenhe>
    pre.fragment
      code.bash.no-bg.
        $ mkdir repos
    pre.fragment
      code.bash.no-bg.
        $ cd repos
  section(data-notes="git config --global core.askpass")
    pre
      code.bash.background.
        $ git clone https://doofenhe@g11007630:3210/doofenhe/masterplan.git
        Password
    pre.fragment
      code.bash.background.
        $ dir

  section(data-background="#282c34").dark
    h3 4. Imperiumsplan ausarbeiten
    p masterplan.json
    pre
      code.json.
        {
          "headquarter": {
            "name":"Doofenschmirtz Tower",
            "city":"Tri-State-Area",
            "ceo":"Heinz Doofenschmirtz"
          },
          "inventions": [
            "Age Accelerator-inator",
            "Copy and Paste-Inator",
            "Melt-inator 6-5000"
          ]
        }
  section(data-notes="Add to staging area")
    h3 5. Sich festlegen
    pre
      code.bash.background.
        $ git status
        On branch master
        Initial commit
        Untracked files:
        (use "git add &lt;file&gt;..." to include in what will be committed)
        masterplan.json
        nothing added to commit but untracked files present 
        (use "git add" to track)
    pre.fragement
      code.bash.background.
        $ git add masterplan.json
  section
    img(src="./img/0/7.svg", alt="")

  section
    pre
      code.bash.background.
        $ git status
        On branch master
        Initial commit
        Changes to be committed:
        (use "git rm --cached &lt;file&gt;..." to unstage)
        new file: masterplan.json
    pre.fragment
      code.bash.background.
        $ git commit -m 'Norms Idea'
        [master (root-commit) 449da69] Initial commit
        1 file changed, 12 insertions(+)
        create mode 100644 masterplan.json
  section
    img(src="./img/0/8.svg", alt="")

  section(data-background="#C1FF9E")
    h3 7. Zum Server senden
  section
    pre
      code.bash.background.
        $ git push origin master
    pre.fragment
      code.bash.background.
        Counting objects: 3, done.
        Writing objects: 100% (3/3), 253 bytes | 0 bytes/s, done.
        Total 3 (delta 0), reused 0 (delta 0) To
         https://YourNameHere@g11007630:3210/YourNameHere/YourNameHere.git
        * [new branch] master -> master
        Branch master set up to track remote branch master from origin.  
  section(data-background="#3191A4")
    h3 8. Updates von Masterserver holen
    pre
      code.bash.background.
        $ git pull --all
    pre
      code.bash.background.
        Fetching origin
        remote: Counting objects: 3, done.
        remote: Compressing objects: 100% (3/3), done.
        remote: Total 3 (delta 0), reused 0 (delta 0)
        Unpacking objects: 100% (3/3), done.
        From https://g11007630:3210/doofenhe/YourNameHere.git
          fe5a280..fcbeeb0 master -> origin/master
        Updating fe5a280..fcbeeb0
        Fast-forward
        YourNameHere | 5 ++++++++++++++
        1 file changed, 5 insertions(+)
